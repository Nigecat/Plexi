import { CommandClient } from "eris";
import * as token from "./config/auth.json";
//import { description } from "../package.json";
//import { prefix, owner } from "./config/config.json";

// Wrap our code in an async functinon so we can use await
(async() => {

    //const client = new CommandClient(token, {}, { prefix, description, owner: `<@${owner}>` })
})();