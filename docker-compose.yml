version: "3"
services:
    bot:
        build: .
        depends_on: 
            - mongo
        environment: 
            - DATABASE_URI=mongodb://mongo:27017/data
        volumes: 
            - .:/plexi
    api:
        build: api/
        ports: 
            - 8000
    mongo:
        image: mongo