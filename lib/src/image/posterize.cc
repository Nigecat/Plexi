#include "image.h"

void Image::posterize(int num_colours)
{
    /*
    std::vector<int> means = Util::random(0, this->width * this->height, num_colours);

    for (int i = 0; i < K_MEANS_NUM_ITERATIONS; i++)
    {
        std::vector<int> indexes = 
        for (int j = 0; j < num_colours; j++)
        {
            //means = mean([p for p, i in zip(ps, indexes) if i == j];
        }
    }
    */
    // TODO
    // Python Implementation of Posterization Algorithm: https://github.com/joelgrus/posterization-pyladies/blob/master/solution/posterization.py
}